# This is a cool project

my project is very cool

## Setup

You can start the project with the start command.

### Building steps

1. Install node.js: `npm i -g node`
2. Install npx: `npm i -g npx`
2. Install dependencies: `npm install`